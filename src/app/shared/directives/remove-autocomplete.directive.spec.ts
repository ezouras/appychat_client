import { RemoveAutocompleteDirective } from './remove-autocomplete.directive';

describe('RemoveAutocompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveAutocompleteDirective();
    expect(directive).toBeTruthy();
  });
});
